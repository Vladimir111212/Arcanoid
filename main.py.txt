import pygame


back = (142, 139, 206) 
move_right = False
move_left = False
start_x = 5
start_y = 5
speed_x = 3
speed_y = 3
platform_y = 300
pygame.init()


class Area():
    def __init__(self, x=0, y=0, width=10, height=10, color=back):
        self.rect = pygame.Rect(x, y, width, height)
        self.fill_color = color
    def color(self, new_color):
        self.fill_color = new_color #устанавливает новый цвет
    def fill(self):
        pygame.draw.rect(mw, self.fill_color, self.rect)
    def out_line(self, frame_color, frame_gage):
        pygame.draw.rect(mw, frame_color, self.rect, frame_gage)
    def collidepoint(self, x, y):
        return self.rect.collidepoint(x, y)
    def colliderect(self, rect):
        return self.rect.colliderect(rect)
    

class Picture(Area):
    def __init__(self, picture, x=0, y=0, width=10, height=10, color=back):
        super().__init__(x=x, y=y, width=width, height=height, color=color)
        self.image = pygame.image.load(picture)

    def draw(self):
        mw.blit(self.image, (self.rect.x, self.rect.y))